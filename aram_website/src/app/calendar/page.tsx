import Calendar from "../_components/Calendar";

export const metadata = {
  title: "Annual Calendar | Aram Tamil School",
  description:
    "Annual calendar of events, classes, holidays, and special days at Aram Tamil School.",
};

export default function CalendarPage() {
  return <Calendar />;
}
